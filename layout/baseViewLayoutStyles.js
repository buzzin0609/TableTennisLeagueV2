/**
 * Description: styles for the BaseViewLayout
 */

import {StyleSheet} from 'react-native';
import constants from '../constants/constants';

export default StyleSheet.create({
	header: {
		backgroundColor: constants.COLORS.PRIMARY
	},
	container: {
		flex: 1
	},
	iconsLight: {
		color: '#fff'
	}
});
