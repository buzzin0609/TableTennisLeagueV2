/**
 * Test suite for encodeData module
 */

import expect from 'expect';
import encodeData from './encodeData';

describe('encodeData:', function () {
	it('should return string of encoded object props and values', function() {
		const data = {
			foo: 'bar'
		};
	})
});